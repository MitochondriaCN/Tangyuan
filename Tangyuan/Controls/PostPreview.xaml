<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Tangyuan.Controls.PostPreview">
    <ContentView.GestureRecognizers>
        <TapGestureRecognizer Tapped="TapGestureRecognizer_Tapped"/>
    </ContentView.GestureRecognizers>
    <Border Stroke="{StaticResource SecondaryBrush}" StrokeThickness="2"
            StrokeShape="RoundRectangle 10,10,10,10" HorizontalOptions="Center" BackgroundColor="White">
        <Grid x:Name="grdMain">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Image x:Name="imgImage" Aspect="AspectFill"/>

            <Grid Grid.Row="1" Margin="8,5,8,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Label x:Name="lblTitle" FontSize="Medium"  FontAttributes="Bold" LineBreakMode="TailTruncation"/>
                <Label x:Name="lblDesc" Grid.Row="1" MaxLines="2"/>
            </Grid>
            <Label Margin="8,5,10,8" x:Name="lblInfo" Grid.Row="2" FontSize="Micro" HorizontalTextAlignment="End" VerticalTextAlignment="End" TextColor="Gray"/>

        </Grid>
    </Border>
</ContentView>
